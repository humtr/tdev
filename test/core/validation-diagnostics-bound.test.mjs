import test from 'node:test';import assert from 'node:assert/strict';import {readFile} from 'node:fs/promises';
test('failed managed checks retain bounded diagnostic context without changing result authority or exposing host credentials',async()=>{const source=await readFile(new URL('../../tools/validate.mjs',import.meta.url),'utf8');assert.match(source,/kind:'validation-check-failure'/);assert.match(source,/diagnosticOnly:true/);assert.match(source,/detail\.length-49152/);assert.match(source,/logDigest:bytesDigest\(log\)/);assert.match(source,/status==='failed'\)code=1/);assert.doesNotMatch(source,/process\.env\.(GITHUB_TOKEN|ACTIONS_ID_TOKEN_REQUEST_TOKEN|DEV2_GITHUB_TOKEN_FILE)/);});

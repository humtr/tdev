# First-release convergence, 2026-09-13

Evidence, not authority. The first release is **not declared complete**. This run
finished the independently available source/provider/native checks. The user's
session constraint excludes tdev public MCP; public mutation fault schedules,
explicit rollback, the current ChatGPT client proof and scored comparison remain
external, alongside physical Android actions. One source trial alone cannot close
all those separate gates. No requirement or evidence layer was waived.

## Exact identities and custody

Freshly fetched canonical `humtr/tdev`, `refs/heads/dev-2`, started and ended at
`5a1637f5f37dbf332f019ede54876362e6b9322b`, parent
`d40359476562a024a949f7af4bc5d11cfa1cee83`, tree
`08f9fce22ba1b7071eb34394f3cde3f89025b992`. Its only difference from the installed
source is WORKBOARD.md. Production enrollment integration `b9c7db0` is already an
ancestor. No production-enrollment implementation was recreated, no multi-account
branch changed, and no canonical history was rewritten.

The original `tdev` checkout, its local `dev2` branch and untracked files were left
untouched. Work used the isolated `dev2-convergence` checkout on
`codex/first-release-convergence-20260913`. Changes are this evidence directory and
a disposable capacity experiment; product source, Designs, authority documents,
public schema and annotation policy were not changed. This evidence is an audit
branch result, not a claim of canonical product integration.

Native ledgers were backed up with the two scoped services stopped, then both
services restarted in `finally`. Reverification used disposable copies and actual
retained content objects. Native validation HMACs, production completion snapshots,
exact Git objects and provider runs were checked. No private database was rewritten
to settle public work. Credentials and raw ledger/provider configuration dumps stay
outside this directory. Hashes identify private raw artifacts without publishing them.

## Validation and live gates

[Initial inventory](inventory-initial.json) and [final inventory](inventory-final.json)
use the four requested states. Each claim names its exercised layer.

- [Local validation](local-validation.json): `npm run check` passed at the canonical
  inputs. Core 175 passed; integration 212 passed, one Android hard-link-creation
  test skipped because the platform prohibits that fixture. Focused release fixtures
  36 passed. The configured aggregate release/live/benchmark profiles remain NOT RUN;
  their descriptor's `implemented=false` was not changed to manufacture PASS.
- [New hosted qualification](current-qualification-result.json): actual GitHub run
  [34754370389](https://github.com/humtr/tdev/actions/runs/34754370389), exact canonical
  source `5a1637f5`, passed containment, managed required profiles and production
  adapter core/integration/release-build. All three production profiles lost one
  completion response and replayed identical output with one launch/execution and
  two deliveries. Actual kernel OOM, deadline, cancellation and containment falsifiers
  passed. The hosted core/integration entrypoint input/output digest is the same
  `dbb98185...` as local validation, under its distinct Linux x64 Node24.21.0 identity.
  This uses fixture completion transport, **not a native OIDC lease or enrollment**.
  Provider mutations created a bounded qualification ref at the exact existing
  canonical commit and deleted that owned ref after success. The 15-minute workflow
  completed successfully; [cleanup](qualification-cleanup.json) preserves the boundary.
- [Enrollment](enrollment-recheck.json): the real retained native OIDC commissioning
  proofs for all three profiles were reverified against exact approved `fa018a4`
  controller/source/runtime and actual output objects. Production seal `efebf458...`
  stays distinct from historical validator seal `672a48...`. Commissioning provider
  run34732066961 eventually ended failure; its three earlier authenticated completed
  assignments are the evidence, not a claim that the entire hosted job succeeded.
- [Builder](builder-recheck.json): the actual production stage artifacts and receipt
  were reverified against exact required validation, prepared result and integrated
  `d403594` source. No second build or production enrollment was needed.
- [Release journal](release-history-recheck.json): native stage `21d907c1...` and
  activation `bc91728f...` join helper `c07ae889...`, its six applied effects and the
  currently deployed pair. This is retained public-operation server evidence, not a
  new current ChatGPT transport/session trial.
- [Provider readback](provider-current.json) and [current boundary verifier](canonical-boundary.json)
  confirm the exact enrolled monotonic ruleset, one matching Access policy,
  workers.dev enabled/previews disabled and the current 100% edge deployment.
  Readback is not a foreign-writer mutation-denial trial or a human OAuth login.

| D0008 gate | Final classification | Exact scope |
| --- | --- | --- |
| A | PROVEN_CURRENT | Current-source real provider adapter qualification plus separately reverified actual native OIDC completion custody. |
| B | PROVEN_CURRENT | Retained private production commissioning; no new commissioning. |
| C | PROVEN_CURRENT | Exact enrollment/source/provider/assignment/result/object join. |
| D | PROVEN_CURRENT | Actual installed helper/native admission and restart health. |
| E | PROVEN_CURRENT | Actual retained managed Builder artifacts reverified. |
| F | PROVEN_CURRENT | Retained public stage action, production build and provider upload. |
| G | PROVEN_CURRENT | Retained public activation and currently observed exact pair. |
| H | PROVEN_CURRENT | Fresh live device/edge paired readback. |
| I | BLOCKED_EXTERNAL | Automatic rollback is proved; explicit rollback trial is not. |
| J | BLOCKED_EXTERNAL | Automatic rollback paired health and current health are proved separately; post-explicit-rollback public readback remains. |

No product defect was repaired and then promoted as proven. Audit-client failures
were localized: Python forwarded probes received non-JSON403 and established no
forwarding proof; the bounded Node fetch replay later exercised the intended read.
A tmcp diagnostic initially selected an unsupported protocol and got400; using the
installed advertised protocol returned200. Both failures remain recorded.

## Concurrency, interruption and Android

[Eight-way evidence](eight-way-recheck.json) joins eight distinct exact-base candidate
works, sixteen authenticated managed completions and eight independently read provider
job intervals. All eight managed launch-to-stop intervals overlap for17.081s and17.734s,
34.815s total. This is retained real execution, not a capacity-setting read or eight
queued controller spans. Cross-host clocks are not independently synchronized; launch
and teardown are included. It does not establish full same-ref W4 completion or a
live failing witness among the eight. Retained B `STALE_RESULT`, then B/C recomposition,
is detailed in [source history](source-history-recheck.json).

[Capacity recovery](capacity-recovery.json) uses current implementation and real
SQLite: admit13 isolated work identities, reserve8, close/reopen the same ledger at
capacity12, retain the original8 reservations, reject the old-owner callback, admit4
more, keep a blocked reservation, release only an unrelated failed reservation and
admit the last queued work. Same-request retry returns the same logical action.
Run `node docs/evidence/first-release-convergence-20260913/capacity-recovery.mjs` from
the repository root to reproduce with disposable state. This does not launch12 jobs
or change installed capacity8. Standard local suites also exercise1/8/16/32.

[Actual Android recovery](android-recovery.json) records native/helper runit down/up
(owner18→19), native SIGKILL/runit restart (19→20), and bounded35s SIGSTOP/SIGCONT.
While stopped, the forwarded read returned `EXECUTION_UNAVAILABLE`, `delivery:unknown`
after30.238s. Reconnect returned the same exact active pair and current canonical head.
No public mutation was invoked during these trials. Pending validation/integration
restart, mutation/integration response loss, same-action duplicate and live conflict
remain distinct public-path acceptance. Current fixtures and the real hosted adapter
replay cover narrower layers and do not replace them.

Whole-Termux force-stop/restart, actual network outage and physical sleep/Doze/reboot
remain user-action gates. App-wide intervention can kill this agent and unrelated
services. Capture the exact request/action and runtime tuple before each physical
trial; after reconnect, observe the same action under the current principal before
retrying the unchanged request. Never infer mutation completion from a timeout.

## Comparison and final client boundary

[Comparison](benchmark-summary.json) separates measured-current tmcp diagnostics,
old-tdev preserved history and analytical comparisons. tmcp.info's retained current
job took168ms admission-to-terminal,78ms execution; its later local HTTP readback
returned7405 bytes. Those clocks are not end-to-end source development. tmcp's configured
capacity4 is not an architectural ceiling. The historical old-tdev microbenchmark
used different source, hardware/runtime and only3 samples. No old runtime was restored.
No paired W1-W11 cohort, p95, superiority ratio or resource-matched throughput is
claimed. D0007's unchanged scored/ChatGPT cohorts still require dev-2 public access.

The previous actual ChatGPT source trial at commit
`21e0c9211ccfd3cde972eb2602d9646cae03d151` is independently corroborated at the native
receipt/Git-effect/ancestry layers. Its original client provenance comes from the
preserved canonical narrative; an independent client transcript was unavailable
here. Codex edits are not counted as a new ChatGPT proof.

The next ChatGPT starting point is `https://tdev.humtr.workers.dev`, fresh discovery
of exactly `dev_context`, `dev_read`, `dev_work`, `dev_observe`. Read canonical identity
and owner epoch again; do not reuse a snapshot/cursor from this audit. First observe
blocked action `d6c07596628e0fc2be45845b4f426c26`: its fixed helper is already terminal
`rolled_back`, while the native action still needs authorized observation/reconciliation.
Do not invent a replacement action or replay its old expired mutation to clear it.

One concrete bounded non-documentation source trial is to add the missing **same-ledger
8→12 capacity/restart regression** to `test/integration/ledger.test.mjs`, independently
implementing the scenario above through ordinary ChatGPT MCP edits. Use current
required managed validation, integrate only that exact prepared result with the fresh
canonical expected head, and obtain authoritative readback. Record the actual client
transcript and unchanged-request duplicate observation. This is a proposed test-source
change, not a source change already performed by Codex. If product-code changes are
required for the experiential interpretation, select a real current defect first;
do not fabricate one or label this evidence document as product self-development.
The source trial does not waive the separate explicit rollback, live interruption,
physical Android or superiority requirements.

## Final installed readback

[Final live observation](final-runtime.json),2026-09-13T11:30:32.004Z:
canonical `5a1637f5...`; device/edge source `d4035947...`; Android arm64 Node24.18.0,
owner20, accepting, capacity8, reserved0/executing0, qualified and sealed;
active release `sha256:3d002c5c417b43502a1d729804a0a988c13aaa716af12bec9a228bd606d7ddce`;
edge version `c3bb4bae-3ea8-4a5d-975e-1b8372fc9de6`. Full separate component digests are
in that artifact. `activeSessions=2` is a retained projection with terminal provider
runs, not two currently executing jobs. Retained staged `60a772...` belongs to the
excluded multi-account lineage and must not be selected for activation or rollback.

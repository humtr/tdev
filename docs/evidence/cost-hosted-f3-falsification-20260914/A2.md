# F3 A2 — validated native one-shot harness and frozen fixture

Status: **F3 remains blocked at native operator execution. F4 not ready.**

This file is non-normative research evidence. It does not change D0001/D0003/D0004, production H2 semantics, public MCP schema, validation requirements or release state.

## Completed since the initial blocker

A one-shot Android/Termux research harness was added at `bench/f3-hosted-harness.mjs` and integrated as commit `478be4c0aa85ace7d4aea996245ca6736b5695e4` after complete current required validation. Harness validation ID: `sha256:5522f80b4e85d29f6716f66b32d14ee78380e8e006c35b827828811ab1a86339`; both `core` and `integration` passed.

The harness is operator-only, fixes repository identity to `github-1322208918` / GitHub repository `1322208918`, fixes the disposable ref to `refs/heads/research/f3-20260914-a2`, rejects canonical/managed-exec targeting, independently verifies current public GitHub repository identity, creates the disposable ref with an absent-ref `--force-with-lease`, and deletes it only with an exact expected-head lease. It wraps the existing durable Git sender so the physical Git process can be held immediately before provider send while its controller is killed. Reopened research ledgers inspect and retry the same logical effect and require the physical invocation identity to remain unchanged.

Three disjoint member candidates and one exact composed candidate were then prepared from the same H `478be4c0aa85ace7d4aea996245ca6736b5695e4`. All four received complete current required validation (`core` + `integration`) and were **not integrated**.

Exact composed identity:

- H: `sha1:478be4c0aa85ace7d4aea996245ca6736b5695e4`
- base tree: `sha1:2d9ab3e382581158672b22fbd27c3a9ae42cd68e`
- C: `sha1:40403f657d07d6588d407dfcdefd0c8bb03cd877`
- U: `sha1:7fa65f94e3e6e1310cddd4b70763145af876407c`
- manifest: `sha256:0dc509a47230a1b693276eaac0f1a0313528586255292bf8953b67c3eb099fef`
- prepared result: `7ed7279f2be55d3a81740b9c579cea73`
- validation: `sha256:9e43090dcdf5b4bad171f0f5b79a9447ad14fa2fa543443b905fb09f81dac22c`
- publication identity: `sha256:2e08cf55ff2fdb6fb550951688cead43a563ffff091d05a9c2b7dd5845e6cdc3`
- logical E: `d7994167ce3e2c9307009e18cbf49ea92b1ab4162c31981e516e87ee2fb44abd`

The complete frozen member tuple is in `fixture-a2.json`.

## Remaining execution blocker

The current authorized ChatGPT `dev-2` surface exposes repository/work/read/observe and hosted `run`/`validate`, but no operation that starts an arbitrary one-shot process in the trusted Termux operator UID. `run`/`validate` execute in `github-hosted-rootless-oci`, where the required profiles are `network:none` and D0005 intentionally withholds the Git writer credential. The GitHub connector available in this session still has no expected-old-ref update primitive and no workflow-dispatch action. Using canonical `dev-2`, `dev2-exec/*`, a non-CAS connector update, or a broad hosted write token would violate the F3 safety boundary.

Therefore the remote dangerous window has still **not** been executed. No disposable research ref has been created yet; cleanup status is none required.

The smallest remaining authorized step is to execute the already validated harness once from the existing trusted Termux operator environment, supplying the installed native config and a private 0600 copy of `fixture-a2.json`. During the harness's held-sender barrier, an unrelated ordinary Work must be admitted/executed through the normal public path to close failure isolation. The harness then performs follower cancellation, controller SIGKILL/reopen twice, same-effect retry, provider release, authoritative ref/ancestry reconciliation, atomic research projection and exact leased cleanup.

Until that process-start capability is available, hosted old-sender survival, response-loss/restart convergence, duplicate-sender count, follower-cancellation containment, atomic all-member terminal projection and uncertain-effect failure isolation remain unknown rather than PASS.

# Environment correction: independent investigation and converged continuation

## Exact starting state and convergence

Fresh start HEAD `0a7e20d128c932a3f2c4ea6b6a544e3f1d705c42`, parent
`8a10ead36deee5f1ba33b0918da6a2cd70c718f8`, tree
`1d2d44967e02c4cb0c48a927ecfd226fe4476a78`; only parentless root
`9a42b05d403370b7b4a698b4d7440d58aa4a79b5`. AGENTS, Directive, Rule, Workboard,
architecture, selected Designs and current source/config/tests were read freshly.
The missing user conditions were Termux/Android as actual first-release control,
workers.dev ingress, and no assumed maintained Linux server/VPS/tunnel or local
server isolation. Directive r2 restores these without choosing a storage/protocol.

An independently prepared correction converged on the same native control / public
workers.dev routing / short-lived hosted-execution allocation as the concurrently
published `415911be09b04c9be22474df7cf2971e996ec9ec`. Its exact owners and changed
implementation were re-read and adopted, not overwritten with duplicate source or
a competing eighth Design. The follow-up remote `88e0bc0ccee0fae3868782a3fc08b0644fe7cf7c`
changed only overview wording and retained CI evidence; it was inspected and
fast-forwarded before this continuation. This evidence records history only.
No predecessor ancestry, architecture authority or published work was discarded.

## Why the selected placement satisfies this user's conditions

Measured native Git/SQLite/private-filesystem/locking/socket/process facilities
support substantive deterministic control and durable work on the phone. They do
not establish hostile-candidate isolation: no usable user namespace/Landlock/OCI
engine was demonstrated. Moving only hostile build/test execution to bounded
managed GitHub sessions preserves the local control role and avoids inventing a
server the user must operate. Existing hosted CI establishes service availability,
not production containment, sufficient quota or comparative speed.

workers.dev is the actual public ingress requirement. An outbound authenticated
phone connection removes the unprovided inbound-network and separate-tunnel premise.
A routing-only rendezvous DO is justified by reaching that connection from unrelated
Worker requests; it owns no work queue, admission truth or integration eligibility.
Local SQLite remains the work owner, Git the canonical ref owner. The managed
session is a resource intent in the existing ledger, not a second development system
or required reasoning model. The first release has no hidden always-on Linux server.

A VPS is unnecessary operating burden. Same-UID/PRoot execution lacks the needed
security evidence. A new Android APK/VM would add an unimplemented native boundary.
Cloudflare Containers entitlement was not observable under available GET permissions;
that is unknown rather than absent. The existing managed GitHub capability is the
selected explicit compute dependency, with queue/startup/data-transfer/idle-cost
and resource-normalized superiority risks retained. It is not an assumed free,
unlimited resource or a promise of proven optimality. No billing change was made.

## Actual observations

`observations.json` contains independently measured Android16/SDK36, Termux
0.119.0-beta.3/arm64, Node24.18.0, Git2.55.0, Python3.14.6 and SQLite3.53.4;
private f2fs, atomic rename/fsync, SQLite WAL reopen, separate-process file lock,
loopback TCP and short-path Unix sockets. Eight subprocesses overlapped and ended
under SIGTERM. CPU count8 and the operation's affinity4 are observations, not an
architectural maximum. A failed absolute Unix socket probe was corrected with a
short relative path; a generic platform.release value was replaced by os.uname
for kernel identity. Hard links were unavailable but verified atomic rename worked.

Doze, app force-stop and device reboot were not forced. No long-lived survival,
hostile-code boundary, eight-work integration throughput or generic Android
capability is inferred from these probes. PRoot is not an isolation proof.

Authenticated Cloudflare GETs confirmed account subdomain humtr, 11 Worker scripts,
existing tdev/tmcp workers.dev routing and predecessor DO/D1 inventory; there was
no observed dev-2 Worker. No new resource hostname was invented. Account token
verification succeeded after using the appropriate account endpoint. Worker/Access
permission metadata was visible; subscription/R2/container queries returned403,
so entitlement/plan remains unknown. `standard` usage is not a paid-plan claim.
Unauthenticated requests to actual predecessor endpoints returned403, not MCP
success. No Worker, Access app, credential, storage, subscription or deployment
was created, changed or deleted. Credentials never appear in the retained files.

## Salvage and concrete implementation changes

Retain runtime-independent F0 identities/codecs/ports, P1 path/grant predicates,
P2 ledger/work/object primitives, P3 exact progressive Git context, P4 isolated
candidate composition and P7 typed inputs/wire. Actual native module tests support
that retention. Do not retain the old generic Linux public broker or phone-side
Podman/systemd assumption. The existing Podman argument adapter remains an
unsealed hosted-execution primitive with negative fixtures only; it is not selected
for native candidate execution and supplies no containment proof.

The converged continuation adds the hosted job OIDC predicate and six negative
contract tests; it strictly compares the persisted session and fresh provider run,
workflow/ref/commit/repository/run-attempt identities. It returns an executor role,
not human mutation authority. Access assertion completion rechecks expiry after
asynchronous key lookup, validates optional nbf and rejects absent app ceilings.
Neither profile silently falls back to permissive scope handling.

A reproduced composition defect caused DeliveryUnavailable to become generic
INTEGRITY_FAILURE through the existing MCP failure encoder, losing same-request
retry guidance. The existing class now uses the bounded domain-error contract with
only `delivery=not_sent|unknown`; the actual encoder preserves that fact and retry.
The before/after observation is in `error-projection.json`. Unknown fact keys and
fabricated successful/failed work outcomes remain rejected.

Validation input inventory now includes deploy/workflow, architecture and toolchain
selection bytes, and output cannot target tracked source. Five convergence tests
cover actual encoder behavior, malformed auth configuration, asynchronous expiry,
optional nbf and disposable byte-changing inventory probes. The obsolete per-core
system-manager/sandbox capability checklist was removed from routine CI, not from
required release proof. Seven Designs remain selected, with bounded D0001/4/5/6/7
clarifications; DIRECTIVE r2, AGENTS and RULE are preserved from the current owner.

## Verification, frontier and limits

`native-core.json` and `validation.json` record actual canonical native core:
120 PASS, 0 FAIL, checked JSDoc and Design consistency PASS. Focused real native
Git/SQLite/candidate integration:32 PASS,0 FAIL. All skipped/cancelled counts are0.
These 32 cases are module evidence, not completion of the broader canonical joined
integration profile. The unimplemented integration/release/live/benchmark profiles
remain NOT RUN with exit2. The final exact source candidate is also checked by the
same Linux CI entrypoint before canonical publication; consult its actual Actions
run, not this text, for that separate commit-specific status.

No dev-2 Worker/OAuth/WS installation, hosted launch/harness isolation, Android
force-stop/reboot, actual ChatGPT typed-union usage, same-ref full-validation cost,
self-update or superiority is claimed. The existing numerical benchmark/trial
requirements were not reduced. Cheap real same-ref/ChatGPT diagnostics precede
expensive cohorts; fixtures are not performance proof. Next work is the published
WORKBOARD's selected adapter/managed-session and validation/integration/MCP joins.

Bootstrap exception: GitHub reads/CI and tmcp project-local native execution/Git
publication were used because dev-2 has no usable canonical development MCP yet.
Only task-owned worktrees and explicit dev-2 publications were mutated. Existing
parallel work and predecessor deployments were preserved. Ordinary development
moves to dev-2 only when its required capability is actually available.

# P1 concrete authorization and rootless launch adapter

Base: b76394c642506bf37c958fe290fd8c1abd38cb36. The retained dev-2 P1 draft
was reused in a separate task-owned branch; no legacy tdev subsystem was imported.
Three tests first failed: scope escalation through a broader standing grant,
missing engine deadline/disabled image defaults, and a create/start response-loss
gap. D0005 was clarified before correction. Verified token scopes now intersect
current grants; absent metadata denies access. The launch plan uses detached run,
fixed entrypoint, no host proxies/image volumes/healthchecks/default environment,
no replacement/restart, engine-owned whole-second timeout and retained input labels.

The adapter explicitly rejects an unavailable rootless cgroup-v2/seccomp engine,
unsealed installation or unresolved launch. It never executes a host fallback.
No production container was launched by the core tests. JWT checks use real JOSE
signatures; command tests spawn real bounded local processes. Podman argument and
observation tests use a deterministic command fixture and do not prove OS isolation.
The release fixture/actual image, sender recovery and canonical installation remain
J2/J3 integration requirements. A created-only uncertain container cannot be blindly
restarted. Full source integrity is supplied by P4 materialization and P5 receipts.

Bootstrap exception remains: canonical dev-2 MCP has not yet been installed, so
project-local tmcp execution and Git publication are necessary. No runtime,
credential, external canonical ref or another task's workspace was modified by
these tests. First failing and corrected TAP logs are retained with this evidence.

Primary implementation references:
- https://docs.podman.io/en/latest/markdown/podman-run.1.html
- https://github.com/panva/jose/blob/main/docs/jwt/verify/functions/jwtVerify.md

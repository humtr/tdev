# dev-2 Design index

> Derived navigation only. This file owns no Design meaning, status, dependency, supersession, or execution order. `DIRECTIVE.md` and `RULE.md` remain above all Designs; each Design owns its declared metadata; `WORKBOARD.md` owns execution routing.

No product Design has been created yet.

When Designs exist, project their metadata here as a table and relationship graph. Do not infer order from Design numbers.

| Design | Title | Status | Depends-On | Supersedes |
| --- | --- | --- | --- | --- |

```mermaid
graph TD
  EMPTY[No Designs yet]
```

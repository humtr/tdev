# D0046 resumption checkpoint

As-of observation on 2026-09-07, derived from D0046@r4 and WORKBOARD; this is not an additional product or routing authority. Evidence: `docs/evidence/group-f-d0046-r4-resume-readback-20260907.json`.

The published source was development@1f03048e101604fc1fb411ebaebad748d6b64be9. Work used a clean isolated checkout; dirty main and tmcp-owned repair worktrees were preserved. This checkpoint is Class 0 evidence/status documentation, with no runtime implementation change.

Authenticated initialization, tool discovery and context read succeeded. Context remains 78f47d5. Both retained Case projections returned HTTP 500 / mcp_internal_error. Temporary existing-Worker tail observations showed the request reached Shared Case; successful invocation outcome does not prove successful Case loading or idle execution. No exception identifying the root cause was exposed. Settings inventory found only the Shared Case self-binding and trial binding to the Case namespace; this does not establish all serving-version consumers or quiescence.

The earlier downloaded-reader local test matched e4420cb exactly and distinguished the legacy invalid_identifier failure from current-reader acceptance. Reuse the earlier 20-row compatibility evidence within its source-corpus limits. Agent source comparison from 581c446 to the observed development source has no src/native/config/package/lockfile changes; no reinstall for SHA alignment.

## Remaining execution

1. Repair or recover the existing authenticated Case/Drive read boundary sufficiently to observe affected active/reconciling execution. Do not infer quiescence from Agent reservation counts, placement inventory, RPC outcome, or failed projections. If implementation changes are needed, classify the precise change under SDD before implementing; do not silently relax D0046 deployment admission.
2. Rebind fresh source and actual serving-version consumers, preserve security/DO/D1 identities, then update Shared Case reader with the required execution safety evidence. Verify retained readability.
3. Align the existing trial and its Drive code to the current source without changing URL, Access or namespaces. Reconcile ambiguous mutations before retrying.
4. Run the original provider M1 candidate/validation lifecycle; then run one real scoped non-documentation development unit through current ChatGPT and read the same durable Case for M2. The currently exposed tdev connector has OAuth experiment probes, not development_unit_start; user registration is needed when M1 is ready.

Do not add diagnostic Worker deployments, preview, version overrides or Access workarounds. Defer exhaustive 19-Case qualification, stress, stable cutover, comparison and self-hosting until after M2. After scoped state is written, retain a compatible reader even if trial admission is rolled back.

No provider activation or model execution is claimed by this checkpoint. The remaining blocker is required owner-state visibility, not missing user login. Historical 502 diagnosis is not a universal prerequisite, but the specific missing execution-safety observation cannot be waived.

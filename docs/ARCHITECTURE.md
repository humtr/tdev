# Deprecated validation-transition sentinel

This file owns no architecture or navigation. It exists only so the currently installed pre-r7 trusted validation controller can validate the transition release that removes this path from the controller's tracked input set. Delete this file immediately after that transition controller is active. Current architecture semantics are owned by accepted Designs.

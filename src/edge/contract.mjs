/** Node/test projection. The reproducible edge build replaces this module with
 * ahead-of-time validators and these exact descriptor bytes. No eval at runtime.
 */
export { TOOL_DESCRIPTORS, SCHEMA_DIGEST, validateOutput } from '../mcp/outputs.mjs';

# dev-2 workboard

Execution state and order only. Authority: DIRECTIVE r2 -> RULE -> selected Designs.
Read AGENTS and these owners at the exact current remote snapshot before resuming.

## Current state

Environment correction is implemented; the next frontier is the revised P1/P2/P5/P6/P7/P8 join below, reusing current F0/P3/P4. The first-release target is native
Termux/Android plus a fixed workers.dev ingress, with no assumed separate VPS/server
or public tunnel. D0005/D0006 now select a native control process, routing-only edge,
and isolated managed execution within the existing GitHub foundation. The previous
generic Linux/systemd/local-Podman topology is no longer an implementation target.
Architecture choices are owned by Designs, not this board.

Already published at correction start: F0 shared contracts and test entrypoint;
corrected P1 authorization/intersection and sandbox argument primitives; P2 durable
work/attempt recovery; P3/P4 bounded Git context and isolated candidates including
10,000-file fixtures; P7 typed inputs and wire codec. These are source modules,
not a completed or installed self-developing product. Preserve their exact contracts
except explicit current Design changes.

Correction implementation: native execution-variant selection, workers.dev binding
validation, bounded reconnect-safe transport core and explicit Access-application
authentication are implemented. Native canonical core passes 109 tests, including
25 new correction tests; focused native Git/SQLite integration passes 32 tests.
Checked JSDoc and Design/document checks pass. These are source/module checks, not
a completed installed gateway or production sandbox. Actual results are recorded in
`docs/evidence/environment-correction-2026-09-11/`. No dev-2 Worker, public origin,
production sandbox seal, activation helper or successful self-development is assumed.

## Selected semantic owners

D0001 work/recovery; D0002 repository/candidates; D0003 validation/integration;
D0004 MCP/observation; D0005 security; D0006 deployment/execution/release;
D0007 verification/superiority. All select DIRECTIVE r2. INDEX is generated from
Design metadata; ID order is not execution order.

## Parallel implementation frontier after this correction

F0 is not restarted. Begin from the published corrected contracts, not a predecessor
branch or an alternative shared-interface implementation. Independent lanes use
separate workspaces and exclusive file scopes; default eight is a work resource
policy, not eight permanent task identities. Shared port/schema changes have one
integration owner and exact review before consumers merge.

| Lane | Exclusive primary scope | Next concrete result / join boundary |
| --- | --- | --- |
| P1 | `src/security/`, `src/execution/` | Access assertion and OIDC/session verification; actual managed sandbox preflight/containment; no local unsafe fallback. |
| P2 | `src/storage/`, `src/work/` | Session intent/run CAS and durable provider-attempt binding; retain current compact candidate pointers and stopped-attempt recovery. |
| P3 | `src/repository/` | Dynamic native Git binding/context, exact remote readback, bounded immutable chunk transfer. |
| P4 | `src/candidate/` | Hosted materialization/reverification using unchanged immutable candidate semantics; no shared writable cache. |
| P5 | `src/validation/`, canonical `tools/validate.mjs` | Full required joined profiles, trusted controller-signed receipts and exact execution seals across device/hosted purposes. |
| P6 | `src/integration/` | Device-side prepared-result/receipt check, scoped same-ref CAS and response-loss reconciliation; real W4 cost observation. |
| P7 | `src/mcp/` | Closed runtime/output schemas, authenticated gateway composition, bounded waits and actual ChatGPT tool transcript. |
| P8 | `src/runtime/`, `src/transport/`, `deploy/`, execution workflow | Actual Worker/DO + outbound device link, bounded managed sessions, Termux launcher and compatible edge/device activation. |

Security profile and transport inputs are established by the current correction
before these lanes join. Do not duplicate a session queue, invent a fixed eight-slot
schema, or let a transport acknowledgement authorize validation/integration.

## Join and evidence sequence

1. Complete the smallest native-control + mocked-provider J1 join early; include
   current P1/P2 fixes and exact prepared-result validation, not an obsolete merge.
2. J2: real isolated managed execution and native control interruption together;
   default 8 overlap, 1/16/32 identity invariance, same-ref complete-validation cost,
   cancellation/lost response/reconnect. A single cheap diagnostic precedes large cohorts.
3. J3: authorized first dev-2 Worker/Access application/device binding provisioning
   and provider readback. Confirm grants, quotas and sandbox seals; a GET permission
   or available predecessor resource is not proof of installation permission.
4. J4: actual ChatGPT canonical-MCP source change -> validation -> integration ->
   terminal readback -> safe component activation, without external task bootstrap.
5. J5: freeze both current predecessor baselines and execute D0007 comparison cohorts.
   Cold session costs and all repeated validation remain inside the measurements.

J1-J5 are incomplete. Native device sleep/app force-stop/reboot, actual dev_work
schema usability, hosted isolation and comparative performance are not unit-test PASS.

## Existing concurrent work and bootstrap exception

The earlier `tmcp/dev2-impl-k47-final` join candidate and P-lane branches are salvage
inputs only. Rebind their exact refs and dirty workspaces before any integration;
extract compatible bounded modules instead of inheriting Linux-host deployment or
replacing current P1/P2 contracts. Preserve all unrelated uncommitted work. Never
merge `development` or predecessor ancestry into dev-2.

GitHub reads/CI, tmcp project-local execution and explicit Git publication are needed
only while dev-2 lacks the required canonical capability. Record each transition
exception. Switch ordinary forward work to dev-2 as capabilities become usable;
provider calls made internally by installed dev-2 are not a second development system.

## Environment correction convergence: implemented continuation

The native/edge/managed-execution topology and DIRECTIVE r2 remain selected. The
additional continuation implements `src/execution/github-identity.mjs`: a strict
hosted-job authentication predicate over the current session and authenticated
provider readback, not a launch controller or completed managed sandbox.

The existing Access verifier now rechecks expiry after asynchronous key resolution,
rejects malformed optional not-before values and missing app ceilings. The existing
rendezvous error now passes through the actual closed MCP error encoder preserving
`EXECUTION_UNAVAILABLE`, `delivery` and same-request retry. Canonical input inventory
now includes deployment/workflow, architecture and toolchain-selection bytes. No
second auth/relay/variant implementation or additional Design owner was retained.

Native canonical core: **120 passed / 0 failed**; focused real Git/SQLite/candidate
module integration: **32 passed / 0 failed**. Checked JSDoc and the seven-Design
consistency check passed. See the exact reports and limitations in
`docs/evidence/environment-correction-2026-09-11/supplement-e91f/README.md`.
These results do not complete the canonical integration profile, J1/J2, production
containment, a workers.dev installation, Android Doze/reboot, actual ChatGPT usage
or predecessor performance comparisons. Their statuses remain NOT RUN / unproven.

Next resume: consume the selected existing native variant/auth/rendezvous modules
and hosted identity predicate; implement the actual Worker/DO + outbound device
adapter, bounded trusted managed-session launch/assignment/receipt, remaining
validation/integration and closed MCP outputs. Follow the lane ownership and join
sequence above. Verify provider budgets/permissions and actual hosted containment
before candidate execution, and test the cheap real same-ref and ChatGPT falsifiers
before expensive repeated benchmark cohorts. No further architecture approval is
pending, and the relevant bootstrap exception remains in effect until dev-2's own
canonical development capability actually works.

The isolated `tmcp/dev2-env-e91f` drafting workspace is retained but **not selected**;
its alternative duplicate modules/Design are not canonical implementation inputs.
Use the published branch, not a dirty parallel workspace or this note's historical
identifiers, as the source of current authority. Existing unrelated work is preserved.

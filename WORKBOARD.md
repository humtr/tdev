# dev-2 workboard

Authority: DIRECTIVE r6 -> RULE -> selected current Designs.

This file owns current execution routing only. It is intentionally resettable. Mutable repository, runtime, provider, Work and Action facts must be freshly rebound from their actual owners; prior chats, plans, evidence and Git history are never current-state authority.

## Release baseline

The first public release was owner-closed on 2026-09-14. D0007 repeated statistical-superiority cohorts and unperformed physical Android sleep/Doze/reboot cells were not converted into PASS by that closure. Production H2 was subsequently enabled and a bounded N=3 production-path observation removed stale recomposition in the exercised shape. Do not reopen H2 architecture or large benchmark cohorts merely because C1 exists.

## Active campaign

- Campaign: `C1` — final `tdev` product convergence and `main` promotion
- Detailed execution plan: `docs/plans/C1-final-tdev-convergence.md`
- Active checkpoint: `C1-1.1`
- Campaign status: active

Campaign/checkpoint IDs are durable trace identities under RULE Section 8. They are not renumbered if later work changes execution order.

| Checkpoint | Objective | Status |
| --- | --- | --- |
| `C1-1` | Cost optimization 2: managed-session terminal reconciliation and lifecycle truth | active |
| `C1-2` | Cost optimization 1: managed-session `UNAUTHORIZED` retry amplification | pending |
| `C1-3` | Single-runtime multi-repository/ref capability | pending |
| `C1-4` | Final product identity cleanup: development codename `dev-2`/`dev2` -> `tdev` | pending |
| `C1-5` | Cost optimization 3: repository-aware source/context transfer deduplication | pending |
| `C1-6` | Final convergence, bounded cost acceptance, exact-state `main` promotion and self-development acceptance | pending |

## Current routing

`C1-1.1` starts by freshly observing the managed-execution/session lifecycle and reproducing or falsifying the retained-session mismatch before mutation. Known historical observations — provider-terminal sessions remaining logically ready/active, one managed-session `UNAUTHORIZED` failure, and repeated full source transfer per required profile — are only investigation hints. Re-establish each fact from current authority before acting.

When a detailed checkpoint completes, advance this file to the next incomplete checkpoint in the same canonical integration as the substantive change when practical. Do not create separate full-validation publications merely to narrate progress. In-flight details belong to durable Work/Action state; enduring procedure belongs to the C1 plan; enduring product meaning belongs to the existing authoritative owner.

## Resume contract

A fresh session must:

1. call current repository context and read `AGENTS.md`, `DIRECTIVE.md`, `RULE.md`, this WORKBOARD and the C1 plan;
2. read only the existing Designs selected by the active checkpoint;
3. freshly observe runtime/provider state and authorized open Work/Action/request state;
4. reconcile those observations with the active checkpoint, treating this file's mutable observations and all prior chat as hints rather than authority; and
5. continue from the first incomplete detailed checkpoint until a plan stop condition, genuine external blocker, or full C1 completion is reached.

No completed campaign plan, evidence directory or history narrative is allowed to become a hidden prerequisite for later ordinary development.
